---
phase: 03-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../the-typescript-narrows/plugin/the-typescript-narrows/.claude-plugin/plugin.json
  - ../the-typescript-narrows/CLAUDE.md
  - ../the-typescript-narrows/.claude/commands/release.md
  - ../the-typescript-narrows/.github/workflows/release-skill.yml
autonomous: true
requirements: [POLSH-01, POLSH-02]

must_haves:
  truths:
    - "plugin.json contains author, homepage, repository, license, and keywords metadata"
    - "All skill repo files reference plugin/v* tag convention instead of skill/v*"
    - "Tag plugin/v1.0.0 exists on the-typescript-narrows repo and is pushed to origin"
  artifacts:
    - path: "../the-typescript-narrows/plugin/the-typescript-narrows/.claude-plugin/plugin.json"
      provides: "Enriched plugin manifest with full metadata"
      contains: "author"
    - path: "../the-typescript-narrows/.github/workflows/release-plugin.yml"
      provides: "Renamed workflow using plugin/v* tag trigger"
      contains: "plugin/v"
  key_links:
    - from: "plugin.json"
      to: "marketplace.json (Plan 02)"
      via: "metadata fields are self-describing; marketplace enriches for discovery"
      pattern: "author.*sethlivingston"
    - from: "git tag plugin/v1.0.0"
      to: "marketplace.json ref field (Plan 02)"
      via: "tag must exist before marketplace can pin to it"
      pattern: "refs/tags/plugin/v1.0.0"
---

<objective>
Enrich the-typescript-narrows plugin.json with professional metadata, rename the tag convention from skill/v* to plugin/v*, and create the plugin/v1.0.0 tag.

Purpose: POLSH-01 (metadata in plugin.json) and POLSH-02 (tag creation for version pinning). The tag must exist before Plan 02 can pin marketplace.json to it.
Output: Enriched plugin.json, updated skill repo docs/workflows, pushed plugin/v1.0.0 tag.
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polish/03-CONTEXT.md
@.planning/phases/03-polish/03-RESEARCH.md

<interfaces>
<!-- Current plugin.json (to be enriched): -->
```json
{
  "name": "the-typescript-narrows",
  "description": "Opinionated TypeScript guidelines for AI-assisted development -- one opinion per decision point",
  "version": "1.0.0"
}
```

<!-- Target plugin.json (from RESEARCH.md Pattern 1): -->
```json
{
  "name": "the-typescript-narrows",
  "description": "Opinionated TypeScript guidelines for AI-assisted development -- one opinion per decision point",
  "version": "1.0.0",
  "author": {
    "name": "sethlivingston",
    "url": "https://github.com/sethlivingston"
  },
  "homepage": "https://github.com/sethlivingston/the-typescript-narrows",
  "repository": "https://github.com/sethlivingston/the-typescript-narrows",
  "license": "MIT",
  "keywords": ["typescript", "style-guide", "code-quality", "ai-coding", "claude-code"]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich plugin.json and rename tag convention across skill repo</name>
  <files>
    ../the-typescript-narrows/plugin/the-typescript-narrows/.claude-plugin/plugin.json
    ../the-typescript-narrows/CLAUDE.md
    ../the-typescript-narrows/.claude/commands/release.md
    ../the-typescript-narrows/.github/workflows/release-skill.yml
  </files>
  <read_first>
    ../the-typescript-narrows/plugin/the-typescript-narrows/.claude-plugin/plugin.json
    ../the-typescript-narrows/CLAUDE.md
    ../the-typescript-narrows/.claude/commands/release.md
    ../the-typescript-narrows/.github/workflows/release-skill.yml
  </read_first>
  <action>
1. **Enrich plugin.json** at `../the-typescript-narrows/plugin/the-typescript-narrows/.claude-plugin/plugin.json`. Replace entire contents with:
```json
{
  "name": "the-typescript-narrows",
  "description": "Opinionated TypeScript guidelines for AI-assisted development -- one opinion per decision point",
  "version": "1.0.0",
  "author": {
    "name": "sethlivingston",
    "url": "https://github.com/sethlivingston"
  },
  "homepage": "https://github.com/sethlivingston/the-typescript-narrows",
  "repository": "https://github.com/sethlivingston/the-typescript-narrows",
  "license": "MIT",
  "keywords": ["typescript", "style-guide", "code-quality", "ai-coding", "claude-code"]
}
```

2. **Update CLAUDE.md** at `../the-typescript-narrows/CLAUDE.md`:
   - Line ~43-44: Change `/release skill 1.1.0` to `/release plugin 1.1.0`
   - Line ~44: Change `/release eslint-plugin 1.1.0 skill 1.1.0` to `/release eslint-plugin 1.1.0 plugin 1.1.0`
   - Line ~47: Change `skill/v{semver}` to `plugin/v{semver}` and update the sentence to say "Plugin" instead of "Skill" for the second tag pattern
   - All other instances of `skill/v` referencing the tag convention should become `plugin/v`

3. **Update release.md** at `../the-typescript-narrows/.claude/commands/release.md`:
   - Line 7: Change `"skill 2.0.0"` to `"plugin 2.0.0"`
   - Line 8: Change `"both 1.1.0"` to `"eslint-plugin 1.2.0 plugin 1.1.0"` (or similar)
   - Line 9: Change `"eslint-plugin 1.2.0 skill 1.1.0"` to `"eslint-plugin 1.2.0 plugin 1.1.0"`
   - Lines 20-24: Replace the "For the skill" section header and all `skill/v*` references with `plugin/v*`
   - Line 22: Change `git tag skill/v{version}` to `git tag plugin/v{version}`
   - Lines 36-37: Update Tag Convention section: change `Skill: \`skill/v{semver}\`` to `Plugin: \`plugin/v{semver}\``

4. **Rename and update release-skill.yml**:
   - Rename `../the-typescript-narrows/.github/workflows/release-skill.yml` to `release-plugin.yml`
   - Change workflow `name:` from `Release Skill` to `Release Plugin`
   - Line 5: Change tag trigger from `'skill/v*'` to `'plugin/v*'`
   - Line 18: Change version extraction from `skill/v` to `plugin/v`: `echo "version=${GITHUB_REF_NAME#plugin/v}" >> "$GITHUB_OUTPUT"`
   - Line 23: Change tag listing from `'skill/v*'` to `'plugin/v*'`
   - Line 41: Change release title from `"Skill v$..."` to `"Plugin v$..."`

5. **Commit all changes** in the skill repo with message: `feat(plugin): add metadata and rename tag convention from skill/ to plugin/`
  </action>
  <verify>
    <automated>cd ../the-typescript-narrows && grep -q '"author"' plugin/the-typescript-narrows/.claude-plugin/plugin.json && grep -q '"homepage"' plugin/the-typescript-narrows/.claude-plugin/plugin.json && grep -q '"license"' plugin/the-typescript-narrows/.claude-plugin/plugin.json && grep -q '"keywords"' plugin/the-typescript-narrows/.claude-plugin/plugin.json && grep -q '"repository"' plugin/the-typescript-narrows/.claude-plugin/plugin.json && ! grep -r 'skill/v' CLAUDE.md .claude/commands/release.md .github/workflows/release-plugin.yml && test -f .github/workflows/release-plugin.yml && ! test -f .github/workflows/release-skill.yml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - plugin.json contains `"author": {` with `"name": "sethlivingston"` and `"url": "https://github.com/sethlivingston"`
    - plugin.json contains `"homepage": "https://github.com/sethlivingston/the-typescript-narrows"`
    - plugin.json contains `"repository": "https://github.com/sethlivingston/the-typescript-narrows"`
    - plugin.json contains `"license": "MIT"`
    - plugin.json contains `"keywords": ["typescript", "style-guide", "code-quality", "ai-coding", "claude-code"]`
    - CLAUDE.md contains `plugin/v{semver}` and does NOT contain `skill/v{semver}` or `skill/v*` (except in the existing eslint-plugin references which are unrelated)
    - release.md contains `plugin/v{version}` and does NOT contain `skill/v`
    - File `release-plugin.yml` exists with `tags: ['plugin/v*']` trigger
    - File `release-skill.yml` does NOT exist
    - `release-plugin.yml` contains `plugin/v` in version extraction and tag listing
  </acceptance_criteria>
  <done>plugin.json has all 5 metadata fields, all skill repo files use plugin/v* convention, workflow file renamed</done>
</task>

<task type="auto">
  <name>Task 2: Create and push plugin/v1.0.0 tag</name>
  <files>None (git tag operation)</files>
  <read_first>
    ../the-typescript-narrows/plugin/the-typescript-narrows/.claude-plugin/plugin.json
  </read_first>
  <action>
1. Change directory to `../the-typescript-narrows`
2. Verify no uncommitted changes exist (Task 1 should have committed)
3. Create tag: `git tag plugin/v1.0.0`
4. Push tag: `git push origin plugin/v1.0.0`
5. Verify tag exists: `git tag -l 'plugin/v*'` should show `plugin/v1.0.0`

Note: Do NOT delete the old `skill/v1.0.0` tag -- leave it for backward compatibility.
  </action>
  <verify>
    <automated>cd ../the-typescript-narrows && git tag -l 'plugin/v1.0.0' | grep -q 'plugin/v1.0.0' && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `git tag -l 'plugin/v1.0.0'` in the-typescript-narrows repo outputs `plugin/v1.0.0`
    - `git tag -l 'skill/v1.0.0'` still outputs `skill/v1.0.0` (old tag preserved)
    - Tag is pushed to origin (verify with `git ls-remote --tags origin` containing `plugin/v1.0.0`)
  </acceptance_criteria>
  <done>Tag plugin/v1.0.0 exists locally and on origin, pointing to current HEAD of the-typescript-narrows</done>
</task>

</tasks>

<verification>
- `cd ../the-typescript-narrows && grep '"author"' plugin/the-typescript-narrows/.claude-plugin/plugin.json` shows author object
- `cd ../the-typescript-narrows && git tag -l 'plugin/v*'` shows plugin/v1.0.0
- No remaining `skill/v` references in CLAUDE.md, release.md, or release-plugin.yml (only eslint-plugin/v references remain, which are correct)
</verification>

<success_criteria>
- plugin.json has author, homepage, repository, license, keywords fields
- Tag convention renamed from skill/v* to plugin/v* in all skill repo files
- plugin/v1.0.0 tag exists and is pushed to origin
- Old skill/v1.0.0 tag preserved
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish/03-01-SUMMARY.md`
</output>
