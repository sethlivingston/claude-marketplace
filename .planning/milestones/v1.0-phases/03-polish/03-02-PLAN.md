---
phase: 03-polish
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - .claude-plugin/marketplace.json
  - .github/workflows/validate.yml
autonomous: true
requirements: [POLSH-01, POLSH-02, POLSH-03]

must_haves:
  truths:
    - "marketplace.json plugin entry includes author, homepage, license, and keywords metadata"
    - "marketplace.json plugin entry pins to refs/tags/plugin/v1.0.0 via source.ref field"
    - "GitHub Actions workflow validates marketplace on push to main and PRs targeting main"
  artifacts:
    - path: ".claude-plugin/marketplace.json"
      provides: "Enriched marketplace manifest with metadata and ref pinning"
      contains: "refs/tags/plugin/v1.0.0"
    - path: ".github/workflows/validate.yml"
      provides: "CI validation workflow"
      contains: "claude plugin validate"
  key_links:
    - from: ".claude-plugin/marketplace.json"
      to: "refs/tags/plugin/v1.0.0 on the-typescript-narrows"
      via: "source.ref field pins to tag created in Plan 01"
      pattern: "refs/tags/plugin/v1.0.0"
    - from: ".github/workflows/validate.yml"
      to: ".claude-plugin/marketplace.json"
      via: "claude plugin validate . checks the manifest"
      pattern: "claude plugin validate"
---

<objective>
Enrich marketplace.json with metadata and ref pinning, and create a GitHub Actions CI workflow for validation.

Purpose: POLSH-01 (metadata in marketplace.json), POLSH-02 (ref pinning to plugin/v1.0.0 tag), POLSH-03 (CI validation). This completes all Phase 3 polish requirements.
Output: Updated marketplace.json with metadata + ref, new .github/workflows/validate.yml.
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polish/03-CONTEXT.md
@.planning/phases/03-polish/03-RESEARCH.md
@.planning/phases/03-polish/03-01-SUMMARY.md

<interfaces>
<!-- Current marketplace.json (to be enriched): -->
```json
{
  "name": "sethlivingston-plugins",
  "owner": {
    "name": "sethlivingston"
  },
  "plugins": [
    {
      "name": "the-typescript-narrows",
      "description": "Opinionated TypeScript guidelines for AI-assisted development -- one opinion per decision point",
      "source": {
        "source": "git-subdir",
        "url": "https://github.com/sethlivingston/the-typescript-narrows.git",
        "path": "plugin/the-typescript-narrows"
      }
    }
  ]
}
```

<!-- Target marketplace.json (from RESEARCH.md Pattern 2): -->
```json
{
  "name": "sethlivingston-plugins",
  "owner": {
    "name": "sethlivingston"
  },
  "plugins": [
    {
      "name": "the-typescript-narrows",
      "description": "Opinionated TypeScript guidelines for AI-assisted development -- one opinion per decision point",
      "author": {
        "name": "sethlivingston",
        "url": "https://github.com/sethlivingston"
      },
      "homepage": "https://github.com/sethlivingston/the-typescript-narrows",
      "license": "MIT",
      "keywords": ["typescript", "style-guide", "code-quality", "ai-coding", "claude-code"],
      "source": {
        "source": "git-subdir",
        "url": "https://github.com/sethlivingston/the-typescript-narrows.git",
        "path": "plugin/the-typescript-narrows",
        "ref": "refs/tags/plugin/v1.0.0"
      }
    }
  ]
}
```

NOTE: Do NOT add "version" or "repository" to the marketplace entry. Version is only in plugin.json (avoids silent conflicts). Repository is redundant with source.url.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich marketplace.json with metadata and ref pinning</name>
  <files>.claude-plugin/marketplace.json</files>
  <read_first>
    .claude-plugin/marketplace.json
  </read_first>
  <action>
Replace `.claude-plugin/marketplace.json` with this exact content:

```json
{
  "name": "sethlivingston-plugins",
  "owner": {
    "name": "sethlivingston"
  },
  "plugins": [
    {
      "name": "the-typescript-narrows",
      "description": "Opinionated TypeScript guidelines for AI-assisted development -- one opinion per decision point",
      "author": {
        "name": "sethlivingston",
        "url": "https://github.com/sethlivingston"
      },
      "homepage": "https://github.com/sethlivingston/the-typescript-narrows",
      "license": "MIT",
      "keywords": ["typescript", "style-guide", "code-quality", "ai-coding", "claude-code"],
      "source": {
        "source": "git-subdir",
        "url": "https://github.com/sethlivingston/the-typescript-narrows.git",
        "path": "plugin/the-typescript-narrows",
        "ref": "refs/tags/plugin/v1.0.0"
      }
    }
  ]
}
```

Key changes from current:
- Added `author` object with name and GitHub profile URL
- Added `homepage` pointing to the skill repo
- Added `license` as "MIT"
- Added `keywords` array with 5 terms
- Added `ref` field in source pointing to `refs/tags/plugin/v1.0.0`
- Did NOT add `version` (only in plugin.json to avoid silent conflicts)
- Did NOT add `repository` (redundant with source.url)
- Kept existing `description` for standalone catalog readability per user decision

Then validate: `claude plugin validate .`
  </action>
  <verify>
    <automated>grep -q '"author"' .claude-plugin/marketplace.json && grep -q '"refs/tags/plugin/v1.0.0"' .claude-plugin/marketplace.json && grep -q '"homepage"' .claude-plugin/marketplace.json && grep -q '"license"' .claude-plugin/marketplace.json && grep -q '"keywords"' .claude-plugin/marketplace.json && ! grep -q '"version"' .claude-plugin/marketplace.json && claude plugin validate . && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - marketplace.json contains `"author": {` with `"name": "sethlivingston"` and `"url": "https://github.com/sethlivingston"`
    - marketplace.json contains `"homepage": "https://github.com/sethlivingston/the-typescript-narrows"`
    - marketplace.json contains `"license": "MIT"`
    - marketplace.json contains `"keywords": ["typescript", "style-guide", "code-quality", "ai-coding", "claude-code"]`
    - marketplace.json contains `"ref": "refs/tags/plugin/v1.0.0"` inside the source object
    - marketplace.json does NOT contain a `"version"` field in the plugin entry
    - marketplace.json does NOT contain a `"repository"` field in the plugin entry
    - `claude plugin validate .` exits 0
  </acceptance_criteria>
  <done>marketplace.json has metadata (author, homepage, license, keywords), ref pinning to plugin/v1.0.0, and passes validation</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions validation workflow</name>
  <files>.github/workflows/validate.yml</files>
  <read_first>
    .claude-plugin/marketplace.json
  </read_first>
  <action>
Create directory `.github/workflows/` if it does not exist.

Create `.github/workflows/validate.yml` with this exact content:

```yaml
name: Validate Marketplace

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Claude Code CLI
        run: npm install -g @anthropic-ai/claude-code

      - name: Validate marketplace
        run: claude plugin validate .
```

Key decisions per user:
- Triggers: push to main + PRs targeting main (no other branches)
- Scope: `claude plugin validate .` only -- no lint, no install test
- Claude CLI installed via npm with no version pin (always latest)
- Node.js 22 (current LTS) for npm availability
- Job name: `validate` (Claude's discretion)
  </action>
  <verify>
    <automated>test -f .github/workflows/validate.yml && grep -q 'claude plugin validate' .github/workflows/validate.yml && grep -q 'push:' .github/workflows/validate.yml && grep -q 'pull_request:' .github/workflows/validate.yml && grep -q 'actions/checkout@v4' .github/workflows/validate.yml && grep -q 'actions/setup-node@v4' .github/workflows/validate.yml && grep -q '@anthropic-ai/claude-code' .github/workflows/validate.yml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/validate.yml` exists
    - Workflow name is `Validate Marketplace`
    - Triggers include `push: branches: [main]` and `pull_request: branches: [main]`
    - Uses `actions/checkout@v4`
    - Uses `actions/setup-node@v4` with `node-version: '22'`
    - Installs `@anthropic-ai/claude-code` globally via npm
    - Runs `claude plugin validate .`
  </acceptance_criteria>
  <done>CI workflow file exists and will validate marketplace manifest on every push to main and PR targeting main</done>
</task>

</tasks>

<verification>
- `claude plugin validate .` passes
- `grep 'refs/tags/plugin/v1.0.0' .claude-plugin/marketplace.json` shows the ref pin
- `.github/workflows/validate.yml` exists with correct triggers and validate step
</verification>

<success_criteria>
- marketplace.json enriched with author, homepage, license, keywords metadata
- marketplace.json plugin source pinned to refs/tags/plugin/v1.0.0
- marketplace.json passes `claude plugin validate .`
- GitHub Actions workflow created for push/PR validation
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish/03-02-SUMMARY.md`
</output>
