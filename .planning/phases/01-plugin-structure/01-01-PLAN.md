---
phase: 01-plugin-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/the-typescript-narrows/.claude-plugin/plugin.json
  - plugin/the-typescript-narrows/skills/the-typescript-narrows/SKILL.md
  - plugin/the-typescript-narrows/skills/the-typescript-narrows/references/
  - plugin/the-typescript-narrows/LICENSE
  - plugin/the-typescript-narrows/README.md
  - CLAUDE.md
  - README.md
  - scripts/generate-traceability.mjs
  - .claude/commands/add-opinion.md
  - .claude/commands/release.md
  - .github/workflows/release-skill.yml
autonomous: true
requirements:
  - PLUG-01
  - PLUG-02

must_haves:
  truths:
    - "The skill repo has a valid .claude-plugin/plugin.json manifest at plugin/the-typescript-narrows/.claude-plugin/plugin.json"
    - "Skill files live at plugin/the-typescript-narrows/skills/the-typescript-narrows/SKILL.md"
    - "All 15 reference files exist under plugin/the-typescript-narrows/skills/the-typescript-narrows/references/"
    - "No references to the old skill/ directory remain in non-.planning files"
    - "claude plugin validate . passes from the plugin root directory"
  artifacts:
    - path: "plugin/the-typescript-narrows/.claude-plugin/plugin.json"
      provides: "Plugin manifest"
      contains: '"name": "the-typescript-narrows"'
    - path: "plugin/the-typescript-narrows/skills/the-typescript-narrows/SKILL.md"
      provides: "Skill definition"
      contains: "name: the-typescript-narrows"
    - path: "plugin/the-typescript-narrows/LICENSE"
      provides: "Plugin license file"
    - path: "plugin/the-typescript-narrows/README.md"
      provides: "Plugin readme"
  key_links:
    - from: "plugin/the-typescript-narrows/.claude-plugin/plugin.json"
      to: "plugin/the-typescript-narrows/skills/the-typescript-narrows/SKILL.md"
      via: "Plugin name matches skill directory name"
      pattern: "the-typescript-narrows"
---

<objective>
Restructure the-typescript-narrows skill repo into a valid Claude Code plugin by renaming skill/ to plugin/, creating the .claude-plugin/plugin.json manifest, reorganizing skill files into the skills/ subdirectory, and updating all path references across the repo.

Purpose: The marketplace (Phase 2) requires a valid plugin structure to reference. This phase transforms the existing skill into that structure.
Output: A valid Claude Code plugin at plugin/the-typescript-narrows/ that passes `claude plugin validate .`
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-structure/01-CONTEXT.md
@.planning/phases/01-plugin-structure/01-RESEARCH.md

IMPORTANT: All work happens in the SKILL REPO at ../the-typescript-narrows/ (NOT the marketplace repo).
The marketplace repo is the current working directory, but file operations target the skill repo.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure files and create plugin manifest</name>
  <files>
    plugin/the-typescript-narrows/.claude-plugin/plugin.json,
    plugin/the-typescript-narrows/skills/the-typescript-narrows/SKILL.md,
    plugin/the-typescript-narrows/skills/the-typescript-narrows/references/,
    plugin/the-typescript-narrows/LICENSE,
    plugin/the-typescript-narrows/README.md
  </files>
  <read_first>
    ../the-typescript-narrows/skill/the-typescript-narrows/SKILL.md,
    ../the-typescript-narrows/skill/the-typescript-narrows/README.md,
    ../the-typescript-narrows/skill/the-typescript-narrows/LICENSE,
    .planning/phases/01-plugin-structure/01-RESEARCH.md
  </read_first>
  <action>
All work happens in the skill repo at ../the-typescript-narrows/. Run these commands in order:

1. Rename parent directory:
   ```
   cd ../the-typescript-narrows && git mv skill plugin
   ```

2. Create skills subdirectory inside plugin root:
   ```
   mkdir -p plugin/the-typescript-narrows/skills/the-typescript-narrows
   ```

3. Move SKILL.md into skills subdirectory:
   ```
   git mv plugin/the-typescript-narrows/SKILL.md plugin/the-typescript-narrows/skills/the-typescript-narrows/SKILL.md
   ```

4. Move references/ into skills subdirectory:
   ```
   git mv plugin/the-typescript-narrows/references plugin/the-typescript-narrows/skills/the-typescript-narrows/references
   ```

5. LICENSE and README.md are already at plugin/the-typescript-narrows/ after step 1 -- no move needed.

6. Create .claude-plugin directory and plugin.json:
   ```
   mkdir -p plugin/the-typescript-narrows/.claude-plugin
   ```

7. Write plugin/the-typescript-narrows/.claude-plugin/plugin.json with EXACTLY this content:
   ```json
   {
     "name": "the-typescript-narrows",
     "description": "Opinionated TypeScript guidelines for AI-assisted development -- one opinion per decision point",
     "version": "1.0.0"
   }
   ```

8. Stage the new untracked files:
   ```
   git add plugin/the-typescript-narrows/.claude-plugin/plugin.json
   ```

Do NOT add metadata fields (author, homepage, repository, license, keywords) -- those are deferred to Phase 3 (POLSH-01).
  </action>
  <verify>
    <automated>cd ../the-typescript-narrows && test -f plugin/the-typescript-narrows/.claude-plugin/plugin.json && test -f plugin/the-typescript-narrows/skills/the-typescript-narrows/SKILL.md && test -d plugin/the-typescript-narrows/skills/the-typescript-narrows/references && test -f plugin/the-typescript-narrows/LICENSE && test -f plugin/the-typescript-narrows/README.md && ls plugin/the-typescript-narrows/skills/the-typescript-narrows/references/*.md | wc -l | grep -q 15 && echo "PASS: All files in correct locations" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - plugin/the-typescript-narrows/.claude-plugin/plugin.json exists and contains `"name": "the-typescript-narrows"`
    - plugin/the-typescript-narrows/.claude-plugin/plugin.json contains `"version": "1.0.0"`
    - plugin/the-typescript-narrows/.claude-plugin/plugin.json contains `"description": "Opinionated TypeScript guidelines`
    - plugin/the-typescript-narrows/skills/the-typescript-narrows/SKILL.md exists and contains `name: the-typescript-narrows`
    - plugin/the-typescript-narrows/skills/the-typescript-narrows/references/ contains exactly 15 .md files
    - plugin/the-typescript-narrows/LICENSE exists
    - plugin/the-typescript-narrows/README.md exists
    - The old skill/ directory no longer exists in the repo
  </acceptance_criteria>
  <done>Plugin directory structure matches the target layout from RESEARCH.md. plugin.json manifest exists with correct name, description, and version. All 15 reference files, SKILL.md, LICENSE, and README.md are in their correct locations.</done>
</task>

<task type="auto">
  <name>Task 2: Update all path references and validate plugin</name>
  <files>
    CLAUDE.md,
    README.md,
    scripts/generate-traceability.mjs,
    .claude/commands/add-opinion.md,
    .claude/commands/release.md,
    .github/workflows/release-skill.yml
  </files>
  <read_first>
    ../the-typescript-narrows/CLAUDE.md,
    ../the-typescript-narrows/README.md,
    ../the-typescript-narrows/scripts/generate-traceability.mjs,
    ../the-typescript-narrows/.claude/commands/add-opinion.md,
    ../the-typescript-narrows/.claude/commands/release.md,
    ../the-typescript-narrows/.github/workflows/release-skill.yml,
    ../the-typescript-narrows/plugin/the-typescript-narrows/README.md
  </read_first>
  <action>
All work happens in the skill repo at ../the-typescript-narrows/. Update every file that references the old `skill/` directory path. The specific changes per file:

1. **CLAUDE.md** (repo root):
   - Line 10 area: Change `skill/` to `plugin/` in the repo layout section
   - Line 47 area: Keep `skill/v{semver}` tag convention as-is (tags are not directory paths; changing would break existing tags)
   - Any other `skill/the-typescript-narrows` paths change to `plugin/the-typescript-narrows`
   - References to SKILL.md path change from `skill/the-typescript-narrows/SKILL.md` to `plugin/the-typescript-narrows/skills/the-typescript-narrows/SKILL.md`
   - References to references/ path change from `skill/the-typescript-narrows/references/` to `plugin/the-typescript-narrows/skills/the-typescript-narrows/references/`

2. **README.md** (repo root):
   - Change all `skill/` directory references to `plugin/`
   - Update any paths to SKILL.md or references/ to reflect new nested structure

3. **scripts/generate-traceability.mjs** (line 113 area):
   - Change `join(ROOT, 'skill', 'the-typescript-narrows', 'references')` to `join(ROOT, 'plugin', 'the-typescript-narrows', 'skills', 'the-typescript-narrows', 'references')`

4. **.claude/commands/add-opinion.md** (lines 155, 185 area):
   - Change `skill/the-typescript-narrows/references/` to `plugin/the-typescript-narrows/skills/the-typescript-narrows/references/`
   - Change `skill/the-typescript-narrows/SKILL.md` to `plugin/the-typescript-narrows/skills/the-typescript-narrows/SKILL.md`

5. **.claude/commands/release.md** (lines 20, 22, 37 area):
   - Keep `skill/v*` tag references as-is (tag convention preserved for backward compatibility)
   - Update any directory path references from `skill/` to `plugin/`

6. **.github/workflows/release-skill.yml** (lines 5, 18, 23, 29 area):
   - Keep `skill/v*` tag pattern triggers as-is (tag convention preserved)
   - Change git log path filters from `skill/` to `plugin/`

7. **plugin/the-typescript-narrows/README.md** (skill-level README now at plugin root):
   - Update any install instructions that reference `skill/the-typescript-narrows/` to reflect the plugin structure

After ALL edits, run a comprehensive grep to confirm no stale `skill/` directory references remain:
```
cd ../the-typescript-narrows && grep -rn 'skill/' --include='*.md' --include='*.mjs' --include='*.yml' --include='*.json' . | grep -v '.planning/' | grep -v 'node_modules/' | grep -v '.git/'
```

Review each remaining hit -- lines containing `skill/v` (tag references) are OK to keep. Lines containing `skill/the-typescript-narrows` are NOT OK and must be fixed.

Finally, validate the plugin:
```
cd ../the-typescript-narrows/plugin/the-typescript-narrows && claude plugin validate .
```

Do NOT skip the .planning/ directory in the skill repo when grepping -- only skip it if it exists. The skill repo may not have a .planning/ directory.
  </action>
  <verify>
    <automated>cd ../the-typescript-narrows && grep -rn 'skill/the-typescript-narrows' --include='*.md' --include='*.mjs' --include='*.yml' --include='*.json' . | grep -v '.planning/' | grep -v 'node_modules/' | grep -v '.git/' | wc -l | tr -d ' ' | grep -q '^0$' && echo "PASS: No stale skill/ directory references" || echo "FAIL: Stale references remain"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `plugin/` in repo layout section (not `skill/` for directory references)
    - CLAUDE.md still contains `skill/v` for tag convention (preserved)
    - scripts/generate-traceability.mjs contains `'plugin'` and `'skills'` in the path join (not `'skill'`)
    - .claude/commands/add-opinion.md contains `plugin/the-typescript-narrows/skills/the-typescript-narrows/references/` (not `skill/the-typescript-narrows/references/`)
    - .claude/commands/add-opinion.md contains `plugin/the-typescript-narrows/skills/the-typescript-narrows/SKILL.md` (not `skill/the-typescript-narrows/SKILL.md`)
    - .github/workflows/release-skill.yml still contains `skill/v*` for tag triggers (preserved)
    - Zero grep matches for `skill/the-typescript-narrows` outside .planning/ and .git/
    - `cd ../the-typescript-narrows/plugin/the-typescript-narrows && claude plugin validate .` exits 0 with no errors
  </acceptance_criteria>
  <done>All path references updated from skill/ to plugin/ (except tag conventions which are preserved). No stale skill/the-typescript-narrows references remain. `claude plugin validate .` passes from the plugin root.</done>
</task>

</tasks>

<verification>
1. Plugin structure is valid: `cd ../the-typescript-narrows/plugin/the-typescript-narrows && claude plugin validate .` passes
2. No stale references: `grep -rn 'skill/the-typescript-narrows' ../the-typescript-narrows/ --include='*.md' --include='*.mjs' --include='*.yml' --include='*.json' | grep -v '.planning/' | grep -v '.git/'` returns 0 results
3. All 15 reference files present: `ls ../the-typescript-narrows/plugin/the-typescript-narrows/skills/the-typescript-narrows/references/*.md | wc -l` returns 15
4. Manifest correct: `cat ../the-typescript-narrows/plugin/the-typescript-narrows/.claude-plugin/plugin.json` shows name, description, version
</verification>

<success_criteria>
- plugin/the-typescript-narrows/.claude-plugin/plugin.json exists with name "the-typescript-narrows", version "1.0.0"
- skills/the-typescript-narrows/SKILL.md and all 15 references are inside the plugin root
- LICENSE and README.md are at the plugin root
- All repo files reference plugin/ instead of skill/ for directory paths
- Tag convention skill/v* is preserved in workflow and release files
- `claude plugin validate .` passes with no errors from plugin root
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-structure/01-01-SUMMARY.md`
</output>
