---
phase: 02-marketplace-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/marketplace.json
  - README.md
  - LICENSE
  - .gitignore
  - CLAUDE.md
autonomous: false
requirements: [MKTPL-01, MKTPL-02, MKTPL-03, MKTPL-04, PLUG-03, PLUG-04]

must_haves:
  truths:
    - ".claude-plugin/marketplace.json exists with name sethlivingston-plugins, owner object, and plugins array"
    - "Plugin entry uses git-subdir source pointing to sethlivingston/the-typescript-narrows at path plugin/the-typescript-narrows"
    - "claude plugin validate . passes with no errors"
    - "User can add marketplace via claude plugin marketplace add sethlivingston/claude-marketplace"
    - "User can install the-typescript-narrows and invoke the skill"
  artifacts:
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace manifest with plugin catalog"
      contains: "sethlivingston-plugins"
    - path: "README.md"
      provides: "Minimal repo description"
      contains: "marketplace"
    - path: "LICENSE"
      provides: "MIT license"
      contains: "MIT License"
    - path: ".gitignore"
      provides: "Standard ignores"
      contains: ".DS_Store"
    - path: "CLAUDE.md"
      provides: "Project conventions for Claude Code"
      contains: "marketplace"
  key_links:
    - from: ".claude-plugin/marketplace.json"
      to: "sethlivingston/the-typescript-narrows"
      via: "git-subdir source with path plugin/the-typescript-narrows"
      pattern: "git-subdir"
---

<objective>
Create the marketplace manifest and repo boilerplate, validate locally, push to GitHub, and verify end-to-end plugin installation.

Purpose: This is the core Phase 2 deliverable -- a working marketplace that users can add to discover and install plugins.
Output: Valid marketplace.json, repo boilerplate files (README, LICENSE, .gitignore, CLAUDE.md), verified remote installation flow.
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-marketplace-catalog/02-CONTEXT.md
@.planning/phases/02-marketplace-catalog/02-RESEARCH.md
@.planning/phases/01-plugin-structure/01-01-SUMMARY.md

<interfaces>
<!-- Plugin manifest from the-typescript-narrows (Phase 1 output) -->
From /Users/sethlivingston/dev/sethlivingston/the-typescript-narrows/plugin/the-typescript-narrows/.claude-plugin/plugin.json:
```json
{
  "name": "the-typescript-narrows",
  "description": "Opinionated TypeScript guidelines for AI-assisted development -- one opinion per decision point",
  "version": "1.0.0"
}
```

<!-- Official marketplace.json schema pattern (from research) -->
Top-level marketplace.json structure:
```json
{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "marketplace-name",
  "description": "Short description",
  "owner": {
    "name": "Owner Name",
    "email": "email@example.com"
  },
  "plugins": [...]
}
```

<!-- git-subdir source format (from official examples) -->
Plugin entry with git-subdir:
```json
{
  "name": "plugin-name",
  "description": "What the plugin does",
  "source": {
    "source": "git-subdir",
    "url": "owner/repo",
    "path": "path/to/plugin/root"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create marketplace manifest and repo boilerplate</name>
  <files>.claude-plugin/marketplace.json, README.md, LICENSE, .gitignore, CLAUDE.md</files>
  <read_first>
    - /Users/sethlivingston/dev/sethlivingston/the-typescript-narrows/plugin/the-typescript-narrows/.claude-plugin/plugin.json (source of truth for plugin name and description)
    - /Users/sethlivingston/dev/sethlivingston/claude-marketplace/.planning/phases/02-marketplace-catalog/02-RESEARCH.md (marketplace.json schema and git-subdir format)
    - /Users/sethlivingston/dev/sethlivingston/claude-marketplace/.planning/phases/02-marketplace-catalog/02-CONTEXT.md (locked decisions on naming, fields, structure)
  </read_first>
  <action>
Create 5 files in the marketplace repo root:

**1. `.claude-plugin/marketplace.json`** -- the main deliverable:
```json
{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "sethlivingston-plugins",
  "description": "Seth Livingston's Claude Code plugins",
  "owner": {
    "name": "sethlivingston"
  },
  "plugins": [
    {
      "name": "the-typescript-narrows",
      "description": "Opinionated TypeScript guidelines for AI-assisted development -- one opinion per decision point",
      "source": {
        "source": "git-subdir",
        "url": "sethlivingston/the-typescript-narrows",
        "path": "plugin/the-typescript-narrows"
      }
    }
  ]
}
```

Key decisions reflected:
- `owner` is an object with `name` field (not a plain string -- per research, the official schema requires an object)
- `owner.email` omitted initially (try without; add if `claude plugin validate .` complains)
- Plugin description included (MKTPL-02 requires it; official marketplace entries all include it; reuses text from plugin.json)
- No `ref` or `sha` in source (version pinning deferred to Phase 3 per user decision)
- GitHub shorthand `sethlivingston/the-typescript-narrows` for URL (confirmed working in official examples)
- Path is `plugin/the-typescript-narrows` (the directory containing `.claude-plugin/plugin.json` in the plugin repo)

**2. `README.md`** -- minimal per user decision:
```markdown
# Claude Marketplace

Seth Livingston's Claude Code plugin marketplace.

## Plugins

See [`.claude-plugin/marketplace.json`](.claude-plugin/marketplace.json) for the current plugin catalog.

## Usage

```
claude plugin marketplace add sethlivingston/claude-marketplace
```
```

**3. `LICENSE`** -- MIT license with copyright "2026 Seth Livingston"

**4. `.gitignore`**:
```
.DS_Store
node_modules/
*.log
```

**5. `CLAUDE.md`** -- project conventions:
```markdown
# Claude Marketplace

This repo is a Claude Code plugin marketplace catalog. It contains a `.claude-plugin/marketplace.json` manifest that references plugins hosted in external repos.

## Structure

- `.claude-plugin/marketplace.json` -- marketplace manifest (the main deliverable)
- `.planning/` -- GSD planning artifacts

## Conventions

- Commit messages: `type(scope): description` (e.g., `feat(marketplace): add plugin entry`)
- Plugin entries use `git-subdir` source type pointing to external repos
- No plugin source code lives in this repo -- catalog only
- Run `claude plugin validate .` after any manifest change
```

After creating all files, run `claude plugin validate .` from the repo root. If validation fails:
- If `owner.email` is required, add `"email": "seth@example.com"` to the owner object (check error message for specifics)
- If any other field is missing, add it based on the error message
- Re-run validation until it passes with no errors

Commit all files: `feat(marketplace): create marketplace manifest and repo boilerplate`
  </action>
  <verify>
    <automated>cd /Users/sethlivingston/dev/sethlivingston/claude-marketplace && claude plugin validate .</automated>
  </verify>
  <acceptance_criteria>
    - .claude-plugin/marketplace.json contains `"name": "sethlivingston-plugins"`
    - .claude-plugin/marketplace.json contains `"source": "git-subdir"`
    - .claude-plugin/marketplace.json contains `"url": "sethlivingston/the-typescript-narrows"`
    - .claude-plugin/marketplace.json contains `"path": "plugin/the-typescript-narrows"`
    - .claude-plugin/marketplace.json contains `"name": "the-typescript-narrows"` in plugins array
    - .claude-plugin/marketplace.json contains `"description":` in plugins array entry
    - README.md contains `marketplace add sethlivingston/claude-marketplace`
    - LICENSE contains `MIT License`
    - .gitignore contains `.DS_Store`
    - CLAUDE.md contains `claude plugin validate .`
    - `claude plugin validate .` exits with no errors
  </acceptance_criteria>
  <done>All 5 files created, marketplace.json passes local validation with no errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Push to GitHub and verify end-to-end installation</name>
  <files>.claude-plugin/marketplace.json</files>
  <read_first>
    - /Users/sethlivingston/dev/sethlivingston/claude-marketplace/.claude-plugin/marketplace.json (verify it exists and is correct before pushing)
  </read_first>
  <action>
Push all committed files to GitHub, then add the marketplace and test installation:

1. Run `git push origin main` to push marketplace.json and boilerplate to GitHub
2. Run `claude plugin marketplace add sethlivingston/claude-marketplace --scope project` to add the marketplace
3. If the push fails, check git remote with `git remote -v` and fix
4. If marketplace add fails, check error output -- common issues: marketplace.json not found on remote (forgot to push), invalid manifest structure (re-validate locally)
5. Present the checkpoint to the user for live install verification (PLUG-04 requires interactive Claude Code session)
  </action>
  <what-built>
Marketplace manifest and repo boilerplate created and validated locally. Now push to GitHub and test the real remote installation flow.

Before the checkpoint, the executor will:
1. `git push origin main` to push marketplace.json to GitHub
2. Attempt `claude plugin marketplace add sethlivingston/claude-marketplace --scope project` to add the marketplace

If the push or marketplace add fails, the executor will debug and fix in-plan.
  </what-built>
  <how-to-verify>
1. Verify marketplace was added: run `claude plugin marketplace list` and confirm `sethlivingston-plugins` or `sethlivingston/claude-marketplace` appears
2. Install the plugin: run `claude plugin install the-typescript-narrows`
3. Start a new Claude Code session and type `/the-typescript-narrows` to verify the skill is available
4. If the skill loads and provides TypeScript guidance, the end-to-end flow is working
  </how-to-verify>
  <verify>
    <automated>cd /Users/sethlivingston/dev/sethlivingston/claude-marketplace && git log --oneline -1 && claude plugin marketplace list</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` shows marketplace files committed
    - `claude plugin marketplace list` includes sethlivingston marketplace
    - `claude plugin install the-typescript-narrows` succeeds
    - `/the-typescript-narrows` skill is invocable in Claude Code
  </acceptance_criteria>
  <done>Marketplace pushed to GitHub, added via CLI, plugin installable, and skill invocable by user</done>
  <resume-signal>Type "approved" if the full flow works, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
- `claude plugin validate .` passes with no errors (MKTPL-01, MKTPL-02, MKTPL-03, PLUG-03)
- Marketplace added from GitHub (MKTPL-04)
- Plugin installed and skill invocable (PLUG-04)
</verification>

<success_criteria>
1. `.claude-plugin/marketplace.json` exists with correct structure, name, owner, and plugin entry
2. `claude plugin validate .` passes with no errors
3. User successfully adds marketplace from GitHub via `claude plugin marketplace add sethlivingston/claude-marketplace`
4. User successfully installs the-typescript-narrows and can invoke `/the-typescript-narrows`
</success_criteria>

<output>
After completion, create `.planning/phases/02-marketplace-catalog/02-01-SUMMARY.md`
</output>
